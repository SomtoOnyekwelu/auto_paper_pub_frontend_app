# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Teaching corpus — local-only. Per the product owner directive this
# folder MUST NOT appear on the public deployment. Renamed 2026-07
# from "teachings to agent" (with spaces) to kebab-case; both spellings
# are excluded so older clones cannot re-introduce a space-laden path.
teachings-to-agent/
"teachings to agent/"

# Remove tracking of claude worktrees, to avoid cloudflare clone repo error of "fatal: No url found for submodule path ...' in .gitmodules"
".claude/worktrees/"
